doctype html
html
        head
            script(src="https://kit.fontawesome.com/f0488b90ea.js", crossorigin="anonymous")
            title #{pageTitle} | #{siteName}
            include ../partials/header
            main
                block content
            include ../partials/footer